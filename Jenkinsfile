pipeline {
    agent any

    stages {
        stage('BRANCH_NAME') {
            steps {
                echo "BRANCH_NAME: ${BRANCH_NAME}"
            }
        }
        stage('BRANCH_IS_PRIMARY') {
            steps {
                echo "BRANCH_IS_PRIMARY: ${BRANCH_IS_PRIMARY}"
            }
        }
        stage('CHANGE_ID') {
            steps {
                echo "CHANGE_ID: ${CHANGE_ID}"
            }
        }
        stage('CHANGE_URL') {
            steps {
                echo "CHANGE_URL: ${CHANGE_URL}"
            }
        }
        stage('CHANGE_TITLE') {
            steps {
                echo "CHANGE_TITLE: ${CHANGE_TITLE}"
            }
        }
        stage('CHANGE_AUTHOR') {
            steps {
                echo "CHANGE_AUTHOR: ${CHANGE_AUTHOR}"
            }
        }
        stage('CHANGE_AUTHOR_DISPLAY_NAME') {
            steps {
                echo "CHANGE_AUTHOR_DISPLAY_NAME: ${CHANGE_AUTHOR_DISPLAY_NAME}"
            }
        }
        stage('CHANGE_AUTHOR_EMAIL') {
            steps {
                echo "CHANGE_AUTHOR_EMAIL: ${CHANGE_AUTHOR_EMAIL}"
            }
        }
        stage('CHANGE_TARGET') {
            steps {
                echo "CHANGE_TARGET: ${CHANGE_TARGET}"
            }
        }
        stage('CHANGE_BRANCH') {
            steps {
                echo "CHANGE_BRANCH: ${CHANGE_BRANCH}"
            }
        }
        stage('CHANGE_FORK') {
            steps {
                echo "CHANGE_FORK: ${CHANGE_FORK}"
            }
        }
        stage('TAG_NAME') {
            steps {
                echo "TAG_NAME: ${TAG_NAME}"
            }
        }
        stage('TAG_TIMESTAMP') {
            steps {
                echo "TAG_TIMESTAMP: ${TAG_TIMESTAMP}"
            }
        }
        stage('TAG_UNIXTIME') {
            steps {
                echo "TAG_UNIXTIME: ${TAG_UNIXTIME}"
            }
        }
        stage('JOB_DISPLAY_URL') {
            steps {
                echo "JOB_DISPLAY_URL: ${JOB_DISPLAY_URL}"
            }
        }
        stage('RUN_DISPLAY_URL') {
            steps {
                echo "RUN_DISPLAY_URL: ${RUN_DISPLAY_URL}"
            }
        }
        stage('RUN_ARTIFACTS_DISPLAY_URL') {
            steps {
                echo "RUN_ARTIFACTS_DISPLAY_URL: ${RUN_ARTIFACTS_DISPLAY_URL}"
            }
        }
        stage('RUN_TESTS_DISPLAY_URL') {
            steps {
                echo "RUN_TESTS_DISPLAY_URL: ${RUN_TESTS_DISPLAY_URL}"
            }
        }
        stage('CI') {
            steps {
                echo "CI: ${CI}"
            }
        }
        stage('BUILD_NUMBER') {
            steps {
                echo "BUILD_NUMBER: ${BUILD_NUMBER}"
            }
        }
        stage('BUILD_ID') {
            steps {
                echo "BUILD_ID: ${BUILD_ID}"
            }
        }
        stage('BUILD_DISPLAY_NAME') {
            steps {
                echo "BUILD_DISPLAY_NAME: ${BUILD_DISPLAY_NAME}"
            }
        }
        stage('JOB_NAME') {
            steps {
                echo "JOB_NAME: ${JOB_NAME}"
            }
        }
        stage('JOB_BASE_NAME') {
            steps {
                echo "JOB_BASE_NAME: ${JOB_BASE_NAME}"
            }
        }
        stage('BUILD_TAG') {
            steps {
                echo "BUILD_TAG: ${BUILD_TAG}"
            }
        }
        stage('EXECUTOR_NUMBER') {
            steps {
                echo "EXECUTOR_NUMBER: ${EXECUTOR_NUMBER}"
            }
        }
        stage('NODE_NAME') {
            steps {
                echo "NODE_NAME: ${NODE_NAME}"
            }
        }
        stage('NODE_LABELS') {
            steps {
                echo "NODE_LABELS: ${NODE_LABELS}"
            }
        }
        stage('WORKSPACE') {
            steps {
                echo "WORKSPACE: ${WORKSPACE}"
            }
        }
        stage('WORKSPACE_TMP') {
            steps {
                echo "WORKSPACE_TMP: ${WORKSPACE_TMP}"
            }
        }
        stage('JENKINS_HOME') {
            steps {
                echo "JENKINS_HOME: ${JENKINS_HOME}"
            }
        }
        stage('JENKINS_URL') {
            steps {
                echo "JENKINS_URL: ${JENKINS_URL}"
            }
        }
        stage('BUILD_URL') {
            steps {
                echo "BUILD_URL: ${BUILD_URL}"
            }
        }
        stage('JOB_URL') {
            steps {
                echo "JOB_URL: ${JOB_URL}"
            }
        }
    }
}